import { part1 } from "./day2"

const input = `
A Y
B X
C Z
`

describe('day 2', () => {
    test('part1', () => {
        expect (part1(input.trim())).toEqual(15)
    })
    test('part2', () => {
        expect (part1(input.trim())).toEqual(15)
    })
})